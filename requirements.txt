# Le coeur (discovery / scrape FullCalendar+TimeKit / compute / sanity) tourne
# en stdlib pure -> aucune dépendance obligatoire.
#
# Optionnel, pour les handlers avancés à venir :
#   playwright        # fallback SPA React (WeChamber, calendriers custom)
#   supabase          # archivage sessions > 30 jours
#
# playwright>=1.44
# supabase>=2.4
